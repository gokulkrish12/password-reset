[build]
  base = "client"
  command = "npm install && npm run build"
  publish = "dist"

# SPA fallback so React Router routes like /reset-password/:token work on refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
